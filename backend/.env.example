# PostgreSQL connection string. Do not commit real credentials.
DATABASE_URL=postgresql+psycopg://fish_game:change-me@localhost:5432/fish_game

# Comma-separated origins allowed to call the API from a browser during development.
CORS_ORIGINS=http://localhost:5173
