# Frontend contributor notes

- Treat FastAPI responses as the authoritative game state.
- Keep API access in a dedicated client module rather than inside page components.
- Use TypeScript types for API payloads and update them alongside backend contract changes.
- Build accessible, keyboard-operable UI before adding visual polish or animation.
